frontend:
  replicaCount: 3

backend:
  replicaCount: 3
  hpa:
    enabled: true
    minReplicas: 3
    maxReplicas: 12
    cpuAverageUtilization: 60

postgres:
  storage: 50Gi

ingress:
  host: cloudcart.example.com
